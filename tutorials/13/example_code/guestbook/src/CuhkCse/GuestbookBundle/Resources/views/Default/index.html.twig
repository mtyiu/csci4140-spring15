<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Symfony Demo Application: Guest Book</title>
		<link href="https://bootswatch.com/paper/bootstrap.min.css" rel="stylesheet">
		<style>
			.row {
				margin-top: 20px;
				margin-bottom: 20px;
			}
			.by {
				padding: 10px;
			}
			.message {
				background-color: #fbfbfb;
				border: 1px dotted #eee;
				padding: 10px;
			}
		</style>
		<!--[if lt IE 9]>
			<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
			<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
		<![endif]-->
	</head>
	<body>
		<div class="container">
			<h3>Symfony Demo Application: Guest Book</h3>
			<hr />
			<div class="row">
				<div class="col-md-4 col-md-offset-8">
					<a href="{{ path( 'cuhk_cse_guestbook_form' ) }}" class="btn btn-success btn-block">Leave a Message!</a>
				</div>
			</div>
			{% for m in messages %}
				<div class="row">
					<div class="col-md-3 by">
						<strong>{{ m.name }}</strong><br />
						<a href="mailto:{{m.email}}">{{ m.email }}</a>
					</div>
					<div class="col-md-9 message">
						<strong>Message</strong> <a href="{{ path( 'cuhk_cse_guestbook_form', { id : m.id } ) }}">[Edit]</a><br />
						{{ m.message }}
					</div>
				</div>
			{% else %}
				<div class="row">
					<p class="lead text-center">No messages</p>
				</div>
			{% endfor %}
		</div>
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
		<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
	</body>
</html>